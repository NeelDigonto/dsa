#include <core/Common.hpp>
#include <core/Tree.hpp>
using namespace std;

class Solution {
 public:
  TreeNode* invertTree(TreeNode* root) {}
};

int main() {
  // auto res1 = Solution{}.invertTree(BinaryTree::deserialize_t({ 4,2,7,1,3,6,9
  // })); auto res2 = Solution{}.invertTree(BinaryTree::deserialize_t({ 2,1,3
  // })); auto res2 = Solution{}.invertTree(BinaryTree::deserialize_t({  }));

  // auto expected = vector<optional<int>>{ 4,7,2,9,6,3,1 };
  // auto expected = vector<optional<int>>{ 2,3,1 };
  // auto expected = vector<optional<int>>{  };

  //TreeNode* root = new TreeNode(1, new TreeNode(2), new TreeNode(3));
  //cout << BinaryTree::serialize_t(root) << endl;
}